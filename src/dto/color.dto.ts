export interface ResponseColorDto {
    id: string;
    name: string;
    code: string;
}

export interface CreateColorDto {
    name: string;
    code: string;
}

export interface UpdateColorDto {
    name: string;
    code: string;
}